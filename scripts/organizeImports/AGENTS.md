# organizeImports

Organizes import statements into `External dependencies` and `Internal dependencies` comment blocks.

```bash
yarn organizeImports          # fix imports in-place
yarn organizeImports --check  # check only, exit 1 if any file needs fixing
```
